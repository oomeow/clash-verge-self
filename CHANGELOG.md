<!--
### 🚨 Breaking Changes

-->

### ✨ Features

- 支持 Preview 预览版更新渠道：CI 每日凌晨定时构建并发布预览版本（`<版本号>-preview.YYMMDDHHMM`），且仅当存在非 bot 的新提交时触发
- 支持在应用内切换更新渠道（Stable / Preview）并获取对应渠道的最新版本
- 更新渠道选项支持多语言显示
- 设置页版本号改为展示应用运行时版本，预览版可正确显示 `-preview` 版本号
- 优化更新检查的资源生命周期，避免重复检查造成泄漏
- 优化全局模式（Global Mode）渲染与侧边栏可见性表现，提升交互体验
- 重构 Proxy 渲染逻辑，以 RenderType 枚举替代魔法数字，结构更清晰
- 简化 Proxy 模块内部实现，移除冗余的 ref 回调包装
- 支持 inline 代理/规则提供者数据渲染
- 重构优化 Mihomo IPC 请求
- 支持在设置页配置滚动日志的大小（MB）与最大保留份数；核心日志于下次启动内核时生效，应用日志于下次重启应用时生效
- 支持 Mihomo 内核版本管理
- 持久化连接界面中的列可见性
- 实时监听 Linux 系统主题变化，动态同步应用主题
- 优化内容页面样式，避免双滚动条
- 重构内核一键升级：改为基于版本索引（mihomo-versions）下载并安装最新版本，自动选择与内置一致的标准编译变体（v3 / 标准版 / armv7 / 386 等），已是最新时自动跳过
- 支持通过 `RUST_LOG` 环境变量控制日志输出级别；未显式设置时沿用应用内日志级别
- 重构更新弹窗与更新流程：优化布局样式（发布信息、预览版标识、下载进度百分比与大小）
- 更新下载支持中途取消；安装完成后由用户手动「重启并应用」，不再自动重启（Windows 下由安装器自动退出并重启）
- 引入 Biome 作为补充 lint 并校准配置，与 ESLint/Prettier 约定对齐；修复由此暴露的问题（内容先转义再高亮的 XSS 加固、正则控制字符、可访问性、polyfill 与构建脚本清理等）
- 抽离 Monaco 编辑器生命周期为共享 Hook（use-monaco-editor），供 profile 编辑器与基础编辑器复用，统一懒加载/销毁、主题与小地图同步，并修复历史 model 泄漏
- 重构 profile 编辑器对话框：拆分为编排层与侧栏/元信息表单/链列表子组件，以 memo + 稳定回调 + FormProvider 隔离输入重渲染，减少击键时对 Monaco 与列表子树的重渲染；并加入日志种子与异步读取的过期结果防护
- 优化无系统标题栏窗口：外圈 2px 描边作为缩放抓手，按指针到边缘/角落的距离实时切换缩放光标（全平台）
- 弹窗挂载到带圆角裁切的根容器，Linux 圆角窗口下随应用形状被裁剪
- 重构可拖拽排序组件 SortableItem（支持分组/禁用、手柄识别、拖拽态样式）；订阅、测试页与链列表排序改为原地高亮 + 统一投影，移除 DragOverlay 浮层
- 调整侧栏 Logo 与更新徽标排布（含折叠侧栏及 macOS）

### 🐛 Bug Fixes

- 修复节点激活流程中的重试机制（activate_selected_nodes），提升稳定性
- 修复 provider 节点延迟检测
- 修复代理节点名称中的 emoji 无法正确渲染
- 修复订阅界面元素溢出时无法滚动
- 修复 scheme 导入链接中 URL 参数被编码导致导入失败
- 修复网格列表拖动排序时出现横向滚动条并闪烁
- 修复更新弹窗发布说明的 Markdown 列表（如 `- 支持…`）不显示符号，避免 Tailwind preflight 影响 MarkdownPreview 样式
- 修复批量操作订阅时底部操作栏遮挡列表末尾
- 修复跑马灯动画周期性跳回起点与悬停变速：改为双副本无缝轨道，位移量随内容尺寸更新，固定滚动时长，悬停暂停/恢复改用 Web Animations API 保持进度
- 修复订阅链列表「整行点击」与「整行拖拽排序」冲突：点击/编辑仅由行内按钮触发
- 修复 SVG 数据 URI 中特殊字符（如中文）无法正常显示的问题。
- 修复更新下载进度条在总大小未知时误按占位值显示确定进度，改为显示「准备中」不确定态。
